// .sequelizerc

const path = require("path");

module.exports = {
  config: path.resolve("src", "db", "config", "db.json"),
  "models-path": path.resolve("build", "db", "models"),
  "seeders-path": path.resolve("build", "db", "seeders"),
  "migrations-path": path.resolve("build", "db","migrations")
};